# include<stdio.h>
int main(){
    int n;
    printf("enter number");
    scanf("%d",&n);
    int product=1,ld;
    while(n>0){
        ld=n%10;
        product=product*ld;
        n=n/10;
    }
    printf("product of digits in a number is %d",product);
    return 0;
}
