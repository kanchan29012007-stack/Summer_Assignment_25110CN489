#include <stdio.h>
int main(){
    int n;
    printf("enter number");
    scanf("%d",&n);
    int sum=0,ld;
    while(n>0){
        ld=n%10;
        sum=sum+ld;
        n=n/10;
    }
    printf("sum of digits in a number is %d",sum);
    return 0;
}
